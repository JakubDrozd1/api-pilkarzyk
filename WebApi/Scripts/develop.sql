--CREATE TABLE AD_HISTORY (
--    ID_AD_HISTORY INTEGER GENERATED BY DEFAULT AS IDENTITY,
--    IDUSER INTEGER NOT NULL,
--    IDAD INTEGER NOT NULL,
--    DATE_CLICK TIMESTAMP);

--CREATE UNIQUE INDEX DATE_QUESTS_ID_AD_HISTORY_IDX
--  ON AD_HISTORY (ID_AD_HISTORY);

--ALTER TABLE AD_HISTORY ADD CONSTRAINT HISTORY_USERS_FK FOREIGN KEY (IDUSER) REFERENCES USERS (ID_USER);
--ALTER TABLE AD_HISTORY ADD CONSTRAINT HISTORY_ADS_FK FOREIGN KEY (IDAD) REFERENCES ADS (ID_AD);


