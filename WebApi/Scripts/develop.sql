--CREATE TABLE MESSAGES_HISTORY (
--    ID_MESSAGE_HISTORY INTEGER GENERATED BY DEFAULT AS IDENTITY,
--    IDMESSAGE INTEGER NOT NULL,
--    BEFORE_CHANGE VARCHAR(50) CHARACTER SET UTF8,
--    AFTER_CHANGE VARCHAR(50) CHARACTER SET UTF8,
--    DATE_CHANGE TIMESTAMP);

--ALTER TABLE MESSAGES_HISTORY ADD CONSTRAINT MESSAGES_HISTORY_PK PRIMARY KEY (ID_MESSAGE_HISTORY);

--ALTER TABLE MESSAGES_HISTORY ADD CONSTRAINT FK_MESSAGES FOREIGN KEY (IDMESSAGE) REFERENCES MESSAGES (ID_MESSAGE) ON DELETE CASCADE;
