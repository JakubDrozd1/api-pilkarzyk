--CREATE TABLE GROUPS_USERS_LINK (
--    IDUSER INTEGER NOT NULL,
--    IDGROUP INTEGER NOT NULL,
--    CODE VARCHAR(40) NOT NULL 
--    );

--CREATE TABLE NOTIFICATION_MESSAGES (
--    ID_NOTIFICATION_MESSAGES INTEGER GENERATED BY DEFAULT AS IDENTITY,
--    IDUSER INTEGER NOT NULL,
--    IDGROUP INTEGER,
--    IDMEETING INTEGER,
--    DATE_SEND TIMESTAMP,
--    BODY VARCHAR(200) CHARACTER SET UTF8,
--    TITLE VARCHAR(100) CHARACTER SET UTF8);

--ALTER TABLE USERS_DATE_QUESTS DROP CONSTRAINT DATE_QUESTS_FK;
--ALTER TABLE ADS ALTER COLUMN COLOR TYPE VARCHAR(70);

--CREATE INDEX GROUPS_USERS_LINK_IDGROUP_IDX ON GROUPS_USERS_LINK (IDGROUP);

--CREATE INDEX GROUPS_USERS_LINK_IDUSER_IDX ON GROUPS_USERS_LINK (IDUSER);

--CREATE UNIQUE INDEX NOTIFICATION_MESSAGES_IDX ON NOTIFICATION_MESSAGES (ID_NOTIFICATION_MESSAGES);

    
--ALTER TABLE NOTIFICATION_MESSAGES ADD CONSTRAINT FK_GROUP FOREIGN KEY (IDGROUP) REFERENCES GROUPS (ID_GROUP);

--ALTER TABLE NOTIFICATION_MESSAGES ADD CONSTRAINT FK_MEETING FOREIGN KEY (IDMEETING) REFERENCES MEETINGS (ID_MEETING);

--ALTER TABLE NOTIFICATION_MESSAGES ADD CONSTRAINT FK_USER FOREIGN KEY (IDUSER) REFERENCES USERS (ID_USER);

--ALTER TABLE GROUPS_USERS_LINK ADD CONSTRAINT GROUPS_USERS_LINK_FK FOREIGN KEY (IDGROUP) REFERENCES GROUPS (ID_GROUP);

--ALTER TABLE GROUPS_USERS_LINK ADD CONSTRAINT GROUPS_USERS_LINK_FK_1 FOREIGN KEY (IDUSER) REFERENCES USERS (ID_USER);
